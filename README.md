# Генератор офферов

Интерактивный калькулятор дохода и генератор офферов на основе шаблона с поддержкой различных структур дохода и автозаполнением типовых обязанностей.

## Особенности

- 🧮 **Интерактивный калькулятор дохода** с поддержкой трех структур:
  - Оклад + MBO
  - Оклад + MBO + БМ (проектная)
  - **Оклад / Оклад + БМ** (новая структура)
- 📝 **Автозаполнение типовых обязанностей** по ролям
- 📊 **Предпросмотр оффера** с расчетом НДФЛ и доходов
- 🏢 **Гибкая настройка** городов, подразделений и рекрутеров
- 📄 **Экспорт в DOCX** с автоматическим именованием файлов

## Быстрый старт (локально)

1. Клонируйте репозиторий и перейдите в директорию проекта:
   ```bash
   git clone https://git-dit.diasoft.ru/estaff/offer-generator.git
   cd offer-generator
   ```

2. Создайте и активируйте виртуальное окружение (venv):
   ```bash
   python3 -m venv venv
   source venv/bin/activate  # Linux/macOS
   # или
   venv\Scripts\activate     # Windows
   ```

3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

4. Запустите приложение:
   ```bash
   streamlit run app.py
   ```

5. Откройте браузер по адресу: `http://localhost:8501`

## Структура проекта

```
offer-generator/
├── app.py                 # Основной код приложения
├── template.docx          # Шаблон оффера
├── config.json           # Настройки рекрутеров
├── cities.json           # Список городов с условиями
├── org_structure.json    # Структура организации
├── typical_duties.json   # Типовые обязанности по ролям
├── requirements.txt      # Зависимости Python
├── Dockerfile           # Инструкция для сборки контейнера
├── justfile            # Сценарии сборки
└── README.md           # Этот файл
```

## Зависимости

- **streamlit** — фреймворк для создания веб-интерфейса
- **docxtpl** — генерация DOCX-документов по шаблону
- **mammoth** — преобразование DOCX в HTML для предпросмотра
- **pymorphy3** — морфологический анализ для склонения имен
- **scipy** — численная оптимизация для обратных расчётов доходов

Все зависимости устанавливаются автоматически:
```bash
pip install -r requirements.txt
```

## Конфигурация

### config.json
Настройки рекрутеров с контактными данными и склонениями имен:
```json
{
  "recruiters": {
    "Имя Фамилия": {
      "position": "должность",
      "position_dative": "должности в дательном падеже",
      "name_dative": "имени в дательном падеже",
      "phone": "номер телефона",
      "email": "email@company.com"
    }
  }
}
```

### cities.json  
Список городов с условиями работы (бонусы ЗОЖ, адреса офисов).

### org_structure.json
Иерархическая структура подразделений компании.

### typical_duties.json
Типовые обязанности для различных ролей с разделением на:
- `обязанности_на_ис` - обязанности в период испытательного срока
- `обязанности` - обязанности по результатам испытательного срока

## Структуры дохода

### 1. Оклад + MBO
Классическая структура с окладом и переменной частью (MBO).

### 2. Оклад + MBO + БМ (проектная)
Расширенная структура с дополнительной бизнес-мотивацией.

### 3. Оклад / Оклад + БМ (новая)
Упрощенная структура только с окладом и опциональной БМ:
- Без MBO и переменной части
- Простое управление через радио-кнопку "БМ: Да/Нет"
- Автоматический расчет эффективного НДФЛ

## Сборка и развертывание в Docker

1. Соберите и запустите контейнер:
   ```bash
   docker build -t offer-generator .
   docker run -p 8501:8501 offer-generator
   ```

2. Или используйте `justfile` для сборки multi-arch образа:
   ```bash
   just build
   ```

## Использование

1. **Заполните основные данные**: ФИО, должность, подразделение
2. **Выберите структуру дохода** и укажите финансовые параметры
3. **Добавьте обязанности** вручную или выберите из типовых
4. **Настройте условия работы**: город, удаленка, гибридный режим
5. **Выберите рекрутера** из списка
6. **Просмотрите оффер** и скачайте DOCX файл

## Разработка

Для внесения изменений:

1. Склонируйте репозиторий
2. Создайте ветку для ваших изменений
3. Внесите изменения в код
4. Обновите соответствующие JSON файлы при необходимости
5. Протестируйте локально
6. Создайте merge request

## Поддержка

Для вопросов и предложений по улучшению обращайтесь к Ивану Савченко (isavchenko@diasoft.ru) или создавайте issues в репозитории (но он их не увидит).